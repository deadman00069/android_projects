<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xddd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main4Activity"></activity>
        <activity android:name=".exerciseActivity" />
        <activity android:name=".selectexerciseActivity">

        </activity>
        <activity android:name=".login_emailActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".homeActivity" />
        <activity android:name=".registerActivity" />
        <activity android:name=".personal_detainActivity" />
        <activity android:name=".welcome" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>