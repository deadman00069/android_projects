<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ui_test4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view_maid_account">

        </activity>
        <activity android:name=".maid_page" />
        <activity android:name=".admin_page" />
        <activity android:name=".login_select" />
        <activity android:name=".update_Buttoon" />

        <activity android:name=".customer" />

        <activity android:name=".add_details_page" />
        <activity android:name=".signup_page" />
        <activity android:name=".login_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>